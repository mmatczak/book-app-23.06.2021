import { InputErrorClassDirective } from './input-error-class.directive';

describe('InputErrorClassDirective', () => {
  it('should create an instance', () => {
    const directive = new InputErrorClassDirective();
    expect(directive).toBeTruthy();
  });
});
